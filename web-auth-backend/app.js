const express=require("express"),bodyParser=require("body-parser"),dotenv=require("dotenv"),cors=require("cors"),connectDB=require("./db"),authRoutes=require("./routes/auth");dotenv.config();const app=express();app.use(cors()),app.use(bodyParser.json()),connectDB(),app.use("/api/auth",authRoutes);const PORT=process.env.PORT||5001;app.listen(PORT,()=>{console.log(`Server running on port ${PORT}`)});